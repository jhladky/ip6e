#include <stdio.h>
#include "l3.h"

/*

So what the testbench is going to do is open a file, full of test packets, and pass each one to the library functions, then we can test for what the results are supposed to be

 */

int main() {

   return 0;
}
